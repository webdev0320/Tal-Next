import { metadataForPath } from '../../lib/seo';
import ConstructionIndustryScheme from '../../views/ConstructionIndustryScheme';

export const metadata = metadataForPath('/construction-industry-scheme');

export default function Page() {
  return <ConstructionIndustryScheme />;
}
